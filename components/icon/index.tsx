import React from 'react'
import Folder from './folder.svg'

const FolderIcon = (): React.ReactElement => {
  return <Folder className="h-10 w-10 text-primary-color-500" />
}

export default FolderIcon
